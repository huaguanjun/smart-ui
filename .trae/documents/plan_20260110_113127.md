1. **修改FullScreenModal组件**：

   * 移除menuItems数据和getCurrentMenu方法

   * 将硬编码的侧边栏菜单替换为插槽

   * 提供slot props（activeMenu和updateActiveMenu方法）供插槽内容使用

   * 保持activeMenu状态管理，用于控制内容显示

2. **更新模板结构**：

   * 在sidebar-menu位置添加slot，命名为sidebar

   * 传递activeMenu和updateActiveMenu作为slot props

   * 保持内容区域根据activeMenu动态显示的逻辑

   * 调整内容头部获取标题和描述的方式

3. **修改FullScreenDemo组件**：

   * 使用新的sidebar插槽提供自定义菜单

   * 利用slot props中的activeMenu和updateActiveMenu管理菜单状态

   * 定义自己的menuItems数据和点击逻辑

   * 确保内容区域能正确显示对应组件

4. **优化样式和交互**：

   * 确保插槽内容能正确应用样式

   * 保持原有菜单的视觉效果

   * 确保点击事件能正确更新activeMenu

   * 测试所有交互功能

5. **保持代码质量**：

   * 保持组件的可复用性

   * 确保样式代码清晰可维护

   * 使用Vue 3的最佳实践

   * 提供清晰的组件文档

