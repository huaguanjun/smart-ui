1. **修改FullScreenModal组件的模板结构**：

   * 使用Vue的transition元素包裹弹窗内容

   * 定义过渡名称，以便应用对应的CSS过渡类

   * 保持现有的clickX和clickY属性传递

2. **修改CSS过渡动画**：

   * 为过渡进入状态应用scaleUp动画

   * 为过渡离开状态应用scaleDown动画

   * 确保遮罩层也有对应的淡入淡出动画

   * 调整动画时序，使关闭动画流畅自然

3. **优化关闭动画效果**：

   * 确保关闭时缩放中心保持为打开时的点击位置

   * 调整scaleDown动画的缓动函数，使其与scaleUp动画协调

   * 优化遮罩层的淡出动画，与弹窗缩小动画同步

4. **测试动画效果**：

   * 测试弹窗打开时的缩放动画

   * 测试弹窗关闭时的缩小消失效果

   * 确保不同位置点击都能正确缩放和缩小

   * 验证动画流畅性和视觉效果

5. **保持代码质量**：

   * 保持组件的可复用性

   * 确保样式代码清晰可维护

   * 使用Vue 3的最佳实践

