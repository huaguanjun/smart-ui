1. **修改FullScreenModal组件**：

   * 添加接收点击位置的props：`clickX`和`clickY`

   * 创建新的缩放动画`scaleUp`，从点击位置开始缩放

   * 修改弹框容器的动画属性，使用新的缩放动画

   * 调整遮罩层动画，与缩放动画协调

2. **修改FullScreenDemo组件**：

   * 添加点击事件处理，记录点击位置

   * 将点击位置传递给FullScreenModal组件

   * 更新组件调用方式，传递新的props

3. **优化动画效果**：

   * 调整动画时长为0.5秒，使用更流畅的缓动函数

   * 确保缩放中心准确，从点击位置开始

   * 保持遮罩层的淡入效果，与缩放动画同步

   * 优化关闭动画，从大缩小回点击位置

4. **验证效果**：

   * 测试弹窗打开时的缩放动画效果

   * 确保不同位置点击都能正确触发从小放大动画

   * 验证关闭动画的流畅性

5. **保持代码质量**：

   * 使用Vue 3 Composition API语法

   * 保持组件的可复用性

   * 确保样式代码清晰可维护

